version: "3.8"
services:
  asterisk:
    image: asterisk:20
    build: .
    network_mode: "host"   # or remove this and use the ports: mapping below
    restart: unless-stopped
    volumes:
      - ./config_local:/config_local
      - ./lib:/var/lib/asterisk
      - ./spool:/var/spool/asterisk
      - ./log:/var/log/asterisk



